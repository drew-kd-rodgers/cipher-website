# Caesar Cipher Encoder/Decoder

Type a message into the plaintext to encode, or the ciphertext to decode.

Key can be adjusted. Direction can be changed with the reverse option, on by default because a traditional caesar cipher goes in reverse.
